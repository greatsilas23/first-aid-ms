import React from 'react'

export default const Rating = (props) => {
    return(
        <Button
            title={this.state.cuts}
            onclick={() => handleRating}
        />

        <Button
            title={this.state.bruises}
            onclick={() => handleRating}
        />
        <Button
            title={this.state.burns}
            onclick={() => handleRating}
        />
    )
}